apiVersion: v2
name: backstage
description: Self-hosted Backstage deployment with PostgreSQL
type: application
version: 0.1.0
appVersion: "1.0.0"

# Bitnami's PostgreSQL chart was removed from public Docker Hub tags as of 2025
# (only signature/metadata blobs remain), so we manage Postgres ourselves with
# a minimal Deployment + PVC + Service in templates/postgres.yaml.
