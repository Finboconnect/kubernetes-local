apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: backstage-dev
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io
  annotations:
    # Track the backstage image. Same pattern as kanban-dev.
    argocd-image-updater.argoproj.io/image-list: backstage=brotherfranklyn/backstage
    argocd-image-updater.argoproj.io/backstage.update-strategy: newest-build
    argocd-image-updater.argoproj.io/backstage.helm.image-name: app.image
    argocd-image-updater.argoproj.io/backstage.helm.image-tag: app.imageTag
    argocd-image-updater.argoproj.io/write-back-method: git:secret:argocd/argocd-image-updater-git
    argocd-image-updater.argoproj.io/git-branch: main
spec:
  project: default

  source:
    repoURL: https://github.com/Finboconnect/kubernetes-local.git
    targetRevision: main
    path: apps/backstage
    helm:
      releaseName: backstage-dev
      valueFiles:
        - values.yaml
        - dev.yaml

  destination:
    server: https://kubernetes.default.svc
    namespace: backstage-dev

  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
      - ServerSideApply=true
